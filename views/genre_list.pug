extends layout

//- block必须顶格
block content
  h1= title

  ul
  each genre in genre_list
    li 
      a(href=genre.url) #{genre.name}
  else
    li There are no book genres in this library.
  